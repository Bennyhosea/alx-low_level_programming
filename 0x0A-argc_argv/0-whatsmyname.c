#include "main.h"
#include <stdio.h>

/**
 * main - a programe that print its name
 * @argc: - argument counter
 * @argv: - argument vector
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
